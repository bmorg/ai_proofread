# cat=backend/general; type=int+; label=Request timeout (seconds): 0 = automatic (180, or 600 for OpenAI-compatible when reasoning is on). Raise PHP max_execution_time to match.
requestTimeout = 0

# cat=backend/general; type=int+; label=Max concurrency: How many LLM calls to run in parallel. (Note that every content element causes an additional call per page.) 0 = automatic (4). Higher is faster but more likely to hit provider rate limits.
maxConcurrency = 0

# cat=backend/general; type=boolean; label=Use mock backend (testing): Return a fake report without calling any API (no key, no cost).
useMock = 0

# cat=provider/general; type=string; label=Base URL: An OpenAI-compatible endpoint (e.g. for Openrouter.ai: https://openrouter.ai/api/v1).
# Default-on: mirrored as a runtime fallback in ExtensionSettings::DEFAULTS (keep in sync).
baseUrl = https://openrouter.ai/api/v1

# cat=provider/general; type=string; label=API key: The API key.
apiKey =
