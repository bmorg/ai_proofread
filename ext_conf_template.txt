# cat=backend/general; type=int+; label=Request timeout (seconds): 0 = automatic (180, or 600 for OpenAI-compatible when reasoning is on). Raise PHP max_execution_time to match.
requestTimeout = 0

# cat=backend/general; type=int+; label=Max concurrency: How many LLM calls to run in parallel. (Note that every content element causes an additional call per page.) 0 = automatic (4). Higher is faster but more likely to hit provider rate limits.
maxConcurrency = 0

# cat=backend/general; type=string; label=Site description: A short noun phrase describing the site, given to the model as context. E.g. "ein Bäckerei-Verzeichnis" or "ein Online-Shop für Fahrräder". Leave empty for a generic website.
siteDescription =

# cat=backend/general; type=string; label=Extra prompt instructions / site-specific rules: Appended to the system prompt for per-site or per-endpoint tuning. E.g. Sei bei Stilfragen besonders zurückhaltend.
extraPromptInstructions =

# cat=backend/general; type=boolean; label=Use mock backend (testing): Return a fake report without calling any API (no key, no cost).
useMock = 0

# cat=provider/general; type=string; label=Base URL: An OpenAI-compatible endpoint (e.g. for Openrouter.ai: https://openrouter.ai/api/v1).
baseUrl = https://openrouter.ai/api/v1

# cat=provider/general; type=string; label=API key: The API key.
apiKey =

# The model and its coupled params (model id, reasoning, max tokens, structured
# output mode, provider routing) are no longer configured here: they live in the
# shipped presets (Bmorg\AiProofread\Enum\ModelPreset) and are switched site-wide
# from the admin-only "Einstellungen" view in the KI-Lektorat module.

# cat=categories/general; type=boolean; label=Check the text for gender-inclusive language ("Gendern") as a category.
enableGenderInclusiveLanguage = 1

# cat=categories/general; type=string; label=Gender-inclusive default: The preferred style for gender-inclusive language, e.g. "Doppelpunkt (z.B. Nutzer:innen)" or "generisches Maskulinum".
genderInclusiveStyle = Doppelpunkt (z.B. Nutzer:innen)

# cat=categories/general; type=boolean; label=Check style: Include optional stylistic suggestions (noisier).
enableStyle = 1
